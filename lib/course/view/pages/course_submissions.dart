import 'package:flutter/material.dart';

class CourseSubmissions extends StatelessWidget {
  const CourseSubmissions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
